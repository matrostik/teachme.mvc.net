@model TeachMe.Models.ResetPasswordViewModel

@{
    ViewBag.Title = "שחזור סיסמא";
}

<h2>שחזור סיסמא</h2>
<p></p>
<p>יש להזין את כתובת הדוא&quotל שאיתה נרשמת לאתר ונשלך אליך קישור לשחזור סיסמא</p>
@using (Html.BeginForm("ResetPassword", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    <hr />
    @Html.ValidationSummary(true)
    <div class="form-group">
        <div class="col-md-11">
            @Html.TextBoxFor(m => m.UserName, new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.UserName)
        </div>
        @Html.LabelFor(m => m.UserName, new { @class = "col-md-1 control-label" })
    </div>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" class="btn btn-default" value="שלך אלי את הקישור" />
        </div>
    </div>
}